//Completa la función que tomando dos números como argumento devuelva el más alto.
function sum(n1, n2) {
  // insert code
  // Comparamos los dos números y devolvemos el más alto
  if (n1 > n2) {
    return n1
  } else {
    return n2
  }
}
